# AI-First CRM

A modern CRM built with React, Vite, FastAPI, and LangGraph.

## Frontend
Located in `frontend/`. 
To run:
```bash
cd frontend
npm install
npm run dev
```

## Backend
Located in `backend/`.
To run:
```bash
cd backend
pip install -r requirements.txt
uvicorn app.main:app --reload
```
